services:
  server:
    build: .
    ports:
      - "3000:3000"
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./static:/static/
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - server